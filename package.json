{
  "name": "server-chat-app",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "client": "npm run start --prefix ../client-chat-app",
    "dev": "concurrently \"npm run start\" \"npm run client\""
  },
  "dependencies": {
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "mysql": "^2.18.1",
    "socket.io": "^4.1.3"
  }
}
